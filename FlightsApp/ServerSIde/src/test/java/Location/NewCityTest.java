package Location;

public class NewCityTest {
    private static final double DELTA = 1;

   /* @Test
    public void testGetAPICityCoordinates() {
        Assert.assertNotNull(London);
        Assert.assertEquals(London.getLocation().lat(), 51, DELTA);
        Assert.assertEquals(London.getLocation().lng(), 0, DELTA);


        Assert.assertEquals(Paris.toString(), "Paris");
        Assert.assertEquals(Paris.getLocation().lat(), 48, DELTA);
        Assert.assertEquals(Paris.getLocation().lng(), 2, DELTA);

        Direction parlon = new Direction(Paris,London);

        Assert.assertEquals(parlon.getDistance(), 343, DELTA);


    }

    public void testCity(City city) {
        Assert.assertNotNull(city);
    }*/
}
